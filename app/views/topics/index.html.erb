<p id="notice"><%= notice %></p>
<h1> <%= t('Topics')%> </h1>
<table>
  <thead>
    <tr>

    <div class="container">


    <th> <%=t('title') %></th>
      <th> <%=  t('description')%></th>
      <th> <%= t('Stars') %> </th>
      <th colspan="3"></th>
    </div>

    </tr>
  </thead>
  <tbody>
hallo
    <% @topics.each do |topic| %>
      <tr>
        <td class="green_text"><%= topic.title %></td>
        <td>  <%= topic.description %></td>
        <td><%= topic.number_of_votes%></td>
        <td><%= link_to t('Show'), topic_path(topic) %></td>
        <td><%= link_to t('Edit'), edit_topic_path(topic) %></td>
        <td><%= link_to t('Destroy'), topic, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td> <%= link_to t('vote'),   new_topic_vote_path(topic) %> </td>

      </tr>
         <% end %>
  </tbody>
</table>

<br>
 <%= link_to t('New Topic'), new_topic_path %>

