<p id="notice"><%= notice %></p>
<div class="blue_text"><h1 > <%= t('Topics')%> </h1> </div>
 <div class="container">

  <div class="columns">
    <div class="one-fourth column" >
      <span class="border text-center d-block p-2 white_text">
        <%=t('title') %>
      </span>
    </div>
    <div class="one-fourth column" >
     <span class="border text-center d-block p-2 white_text" > <%=  t('description')%>

     </div>
    <div class="one-fourth column" >
    <span class="border text-center d-block p-2 white_text">
    <%= t('Stars') %>
    </div>
    <div class="one-fourth column"></div>
  </div>
  </div>
  <% @topics.each do |topic| %>
    <div class="width_letters">
      <div class="columns">
        <div  class="one-fifth column white_text" ><%= topic.title %></div>
        <div class="one-fifth column white_text" >  <%= topic.description %></div>
        <div class="one-fifth column white_text"><%= topic.number_of_votes%></div>
        <div class="two-fifth column"></div>
      </div>
      <div class="btn-group">
        <%= link_to t('Show'), topic_path(topic), class: "btn btn-sm" %>
        <%= link_to t('Edit'), edit_topic_path(topic), class: "btn btn-sm" %>
        <%= link_to t('Destroy'), topic, class: "btn btn-sm btn-danger", method: :delete, data: { confirm: 'Are you sure?' } %>
        <%= link_to t('vote'),   new_topic_vote_path(topic) , class: "btn btn-sm btn-primary"%>
      </div>
      <div class="image"></div>​
    </div>
  <% end %>
  <div class="background">
  </div>
</div>

<br>
 <%= link_to t('New Topic'), new_topic_path%>

 </div>

